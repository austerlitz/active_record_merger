# frozen_string_literal: true

RSpec.describe ActiveRecordMerger do
  it "has a version number" do
    expect(ActiveRecordMerger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
